# 开发规范 — 个人 API 密钥保险箱

## 1. 总体约束
- **语言**：Python 3.10+；核心模块 `vault.py` ≤ 500 行
- **依赖**：`cryptography`、`sqlite3`（标准库）、`argparse`、`rich`（可选）
- **结构**：
  - `vault.py` — 加解密核心（scrypt+AES-GCM）
  - `store.py` — SQLite 持久化
  - `cli.py` — 全部子命令
  - `audit.py` — .env 明文扫描
- **安全红线**：
  - 主密钥**永不写盘**；只在解锁后的进程内存
  - 密文库用 0600 权限创建
  - 明文值只在**显式请求**（`get`/`use`）时 stdout 输出，`list` 永不含值

## 2. 加密模型
- 初始化：`kv init` → 用户输入主密码 → `scrypt(N=2^15, r=8, p=1)` 派生 32B key + 随机 salt 存入库头
- 每条 entry：`AESGCM(key, nonce=os.urandom(12))` → `ciphertext` 且独立 nonce；`associated_data = name+provider` 防篡改
- 存储：SQLite 表：
```
secrets(id, name TEXT UNIQUE, provider TEXT, ciphertext BLOB, nonce BLOB, created_at, expires_at, notes)
```
- 解锁：`kv unlock --passphrase <env:KV_PASS>`；进程退出锁自动失效

## 3. CLI 接口（硬性）
```
kv init                    # 创建 vault，要求主密码 ≥8 位（不强制复杂但提示）
kv add <name> --provider deepseek [--expires 2026-12-01]  # stdin 或 --v 取密钥
kv get <name>              # 解密，打印...仅显示后 4 位（--full 才全显）
kv list                    # 名称/供应商/过期日期（永不有值）
kv use <name>              # 输出 APP_KEY=xxx 导出行（供 eval / 注入 .env 文件）
kv edit <name>
kv delete <name>           # 需二次确认
kv audit --dir .           # 扫描 .env/配置明文密钥
kv rotate --provider github # 打印该供应商轮换 URL（模板化）
kv export-backup <file>    # 导出加密备份
```
- `--yes` 只能用于已确认安全的 `delete`/`rotate`；禁止无条件批量删

## 4. `use` 注入设计（安全）
- 默认打印 `export DEEPSEEK_API_KEY=sk-xxxx` 行（`--shell bash/zsh/fish` 自适应）
- 支持 `--dotenv out.env`：写入 0600 权限的临时 .env，用完删；提示不再明文留
- **禁止** `use` 直接 print 值到被 shell history 捕获的场景（记录到 README）

## 5. audit 扫描
- 递归扫 `.env*`、`*.ini`、`*.toml` 的 `KEY=value`，探测常见 key 名（OPENAI/DEEPSEEK/GITHUB_TOKEN…）
- 命中 → 只显示文件名+key 名+一段明文（或 `:REDACTED`），绝不打印密钥值
- `--fix-0600` 可选修复权限
- `--no-write` 默认只报告

## 6. 容错与安全
- 主密码错误：提示“无法解锁”（不泄露 salt 存在性差异）
- 写入先写到 `DEFAULT CACHE` 再原子 rename（防半写）
- 异常时密文绝无明文备份

## 7. 测试
- `tests/test_vault.py`：init/unlock/list/get/delete/audit；**密文找不到明文 key**、nonce 独立性、篡改检测
- `tests/test_audit.py`：制造带 .env 的样例目录，命中报告正确、不打印值
- `tests/test_cli.py`：参数/输出格式/二次确认
- `pytest` 全绿（`-v`）

## 8. 交付物
- CLI 可运行 + README（安装、使用、安全模型、免责：主密码丢失不可恢复）
- `pyproject.toml` 安装为 `kv`
- 示例演示截图