name: Test

on:
  workflow_dispatch:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e .
          pip install pytest pytest-cov

      - name: Run tests with coverage gate
        # Coverage omit for keyvault/gui.py lives in pyproject.toml (display-dependent;
        # headless CI was 63.7% < fail-under=65).
        run: pytest tests/ -v --cov=keyvault --cov-report=term --cov-fail-under=65

      - name: Dependency audit
        run: |
          pip install pip-audit
          pip-audit --skip-editable
