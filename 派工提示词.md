# 派工提示词 — KeyVault（个人 API 密钥保险箱）

> 依据「派工提示词 — 通用模板（v1）」为 projects/06-API密钥保险箱 填写。
> 形态：单 Agent 全栈实现（贴合模板"一条指令、一个独立 AI"主旨）；如需按「架构设计.md 第6节」拆 AI-A/B/C/D 多 Agent 并行派发，见文末附注。

---

【角色】你是独立软件工程师，负责从零实现一个工具，全程自主决策，不向用户提问。

【项目定位】
- 项目名：个人 API 密钥保险箱（KeyVault）
- 一句话描述：给开发者一个极简的本地加密钥匙串——专管 LLM API Key、GitHub Token、云密钥，比 1Password 轻、比 .env 安全
- 所属目录：projects/06-API密钥保险箱/（绝对路径：C:\Users\30816\Desktop\思路\projects\06-API密钥保险箱\）
- 文档位置：4 份文档全文已内嵌于本文末「附录：四份项目文档全文」，目标环境无需访问原路径即可开工
  1. 思路.md —— 产品背景、证据、边界与免责
  2. 开发规范.md —— 硬性规范（接口、安全、测试、交付物）
  3. 开发规划.md —— 分阶段计划与验收清单
  4. 架构设计.md —— 模块划分、接口契约、并行切分

【工作方式】
1. 开工前先完整阅读上述 4 份文档；如有矛盾，以「架构设计.md > 开发规范.md > 开发规划.md > 思路.md」为优先级顺序。
2. 完整实现全部模块（单 Agent 全栈，不拆分）：按「架构设计.md」第 6 节装配顺序推进——① vault.py（密码学核心，率先冻结）→ ② store.py 与 audit.py → ③ cli.py + config.py 组装；**绝不修改接口契约**（dataclass / 函数签名 / SQLite 表结构 / CLI 参数 / 退出码 / 模块职责边界）。
3. 依次完成「开发规划.md」中的 Phase 1→2→3→4（加密核心 → CLI 骨架 → audit/rotate/体验 → 发布），每阶段末运行该阶段验证命令并记录结果。
4. 只使用文档声明的依赖：cryptography、sqlite3（标准库）、argparse、rich（可选）；新增任何第三方依赖前先说明理由（文档未允许的一律不加）。
5. 全程不向用户提问；遇到歧义 → 选择文档规定的最保守/最安全方案，并在交付说明中标注。

【安全红线】
通用：
- 密钥/Token 一律从 stdin 或环境变量（KV_PASS）读取，绝不硬编码、绝不写入任何文件
- 本工具对用户数据默认只读；涉及写盘（delete / edit / import-backup）必须遵循文档的二次确认 / 原子写 / 备份要求
- 不得发送用户原始敏感数据到第三方（本项目无任何网络调用点，rotate 仅打印轮换 URL 指引）

项目特有（不可绕过）：
- 主密钥**永不写盘**：只封存在解锁后的进程内存，进程退出即失效
- 密文库以 0600 权限创建；任何写操作前校验权限
- 明文值只在显式请求时输出：kv get 默认只显后 4 位（--full 才全显）、kv use 只输出 export 行 / .env 文件；kv list 永不含任何值
- audit 只读扫描，命中只报「文件名 + key 名 + 打码摘要」，绝不打印密钥值；--fix-0600 由 cli 控制，audit 本身不写盘
- kv delete 必须二次确认；--yes 仅允许 delete / rotate，禁止无条件批量删
- 不提供"显示全部明文"的批量功能

【代码规范】
- 语言/依赖/目录结构严格按「开发规范.md 第1节」：Python 3.10+，vault.py（≤500 行）/ store.py / cli.py / audit.py / config.py / gui.py
- 模块职责与禁止出口（架构设计.md 第1节）：vault 不打印明文；audit 不写任何文件；store 不 import audit；依赖方向 cli → vault/store/audit，gui → vault/store/config（gui 不 import cli，避免循环）
- 不加文档中未列明的注释（代码维持最小注释，必要时只加文档字面要求）
- 所有公开函数按契约命名，docstring 只写用途一句话

【GUI 前端契约（新增需求，gui.py）】
- 形态：tkinter 桌面前端（标准库，零新依赖），由 `kv gui` 子命令启动（cli 延迟 import gui，无显示环境不影响其他命令）
- 功能：解锁 / 列表（Treeview 表格，永不含值）/ 单条查看·复制（显式操作）/ 添加 / 编辑 / 删除（二次确认）/ 导出·导入加密备份 / vault 不存在时创建向导
- 安全：主密钥仅存 GUI 进程内存（实例属性）；密码输入框 show="*"; 删除/导入弹窗二次确认; 列表永不含明文（与 kv list 同底线）；不提供批量明文导出；表单校验与 CLI 同源（日期 YYYY-MM-DD、重名拦截），错误弹窗提示
- 可测试性：表单逻辑（validate_expires / mask / is_expired / add_entry / update_entry）必须为不依赖 Tk 的纯函数，供 tests/test_gui.py 在任何环境（含无 display CI）测试；GUI 实例化冒烟在无 display 时 skip
- 主密码错误统一提示「无法解锁」（与 CLI 一致，不泄露 salt 存在性差异）

【测试要求】
- 每个模块对应测试文件：tests/test_vault.py、tests/test_audit.py、tests/test_cli.py、tests/test_gui.py；pytest 全绿
- 测试不得依赖网络（可用 mock/stub）
- 集成测试仅用于验证 CLI 接口（命令参数 / 输出格式 / 二次确认），不跑长链路
- 必须覆盖（开发规范第7节 + 架构设计第7节 + GUI 契约）：加解密往返、篡改→报错、nonce 独立、密文搜不到明文、audit 命中报告且不打印值、list 无值、CLI 输出格式 / 二次确认、export/import 往返、GUI 表单校验（空值/重名/非法日期）、GUI 列表不含值、GUI 实例化冒烟

【完成后交付内容（按此格式输出）】
1. 变更清单：实现了哪些模块（vault/store/audit/cli/config/tests），各阶段验证结果 + 证据
2. 使用示例：至少 3 个命令/调用示例（含关键输出）
3. 部署/运行说明：依赖安装命令、环境变量设置（KV_PASS）、测试运行命令
4. 与规范偏差表：若有偏离文档决策的项，列明与理由
5. 已知限制与本阶段未做的功能（按「开发规划.md」勾选）

【验收清单】
- [ ] pytest tests/ -q 全绿（或 pytest -v）
- [ ] 交付示例逐条可运行
- [ ] 没有未声明的网络调用（本项目应为零网络调用）
- [ ] 无通过测试绕过（如跳过契约验证 / 篡改检测）
- [ ] README 已按「开发规范.md 第8节」生成：安装、使用、安全模型（scrypt+AES-GCM 工作原理）、主密码丢失 = 数据不可恢复警示、use 注入不落 shell 历史的说明、示例全流程
- [ ] pyproject.toml 已将 CLI 安装为 kv
- [ ] `kv gui` 可正常启动（无 display 环境跳过；Windows 上冒烟实例化通过）
- [ ] vault.py ≤500 行，其余模块规模与功能匹配
- [ ] GUI 列表（Treeview）不含任何明文值；查看/复制为单条显式操作

【接口核对清单（验收关口，逐条输出核对结果）】
- [ ] derive_key 同密码 + 盐幂等；不同盐 → 不同 key（有测试）
- [ ] 篡改 ciphertext / tag → decrypt 抛 ValueError
- [ ] audit 输出与文件内容 0 写（全程只读）
- [ ] export/import 往返成功且校验 header
- [ ] list 输出永远无明文（测试对值区断言为 None）
- [ ] GUI 表单校验与 CLI 同源：非法日期 YYYY-MM-DD / 重名 / 空值均拒绝（有测试）
- [ ] GUI 主密码错误 → 统一「无法解锁」提示

---

## 附注：多 Agent 并行拆分方案（如需）

按「架构设计.md 第6节」切分，每个 Agent 拿本文档 + 对应模块契约：

- **AI-A**：vault.py（密码学核心，率先冻结，全项目依赖它）
- **AI-B**：store.py（原子 IO + 测试，用 EncryptedEntry 类型占位，接口以架构设计.md 2.2 为准）
- **AI-C**：audit.py（只读扫描 + 测试）
- **AI-D**：cli.py + config.py（组装，依赖 A/B/C 完成后联调）
- **AI-E**：gui.py（tkinter 前端，依赖 A/B/D 完成后；表单逻辑为纯函数，另随 tests/test_gui.py）

装配时序：A → (B, C) 并行 → D → E。

---

## 附录：四份项目文档全文

> 以下为 4 份源文档的逐字全文（嵌入于 2026 年派发时快照）；若与你目标环境中的同名文件存在差异，以本文内嵌全文为准，并以「架构设计.md > 开发规范.md > 开发规划.md > 思路.md」为矛盾裁决顺序。

### 附录 1：思路.md

```markdown
# 个人 API 密钥保险箱（KeyVault）

> 一句话：给开发者一个极简的本地加密钥匙串——专管 LLM API Key、GitHub Token、云密钥，比 1Password 轻、比 .env 安全。

## 一、项目思路

### 1. 要解决的问题
- 开发者手上有 5-30 个 API Key/Token（DeepSeek、OpenAI、GitHub、云 AK、内网 DB 密码……）散落各处：.env、备忘录、聊天记录、Shell 历史
- 主流密码管理器（1Password/Bitwarden）太重且不面向"API Key 场景"（没有模板、没有复制 OAuth token 功能、没有 rollover 提醒）；企业 vault（Infisical/EnvKey）面向团队基建
- `.env` 文件明文裸奔是常态，泄露一次就是事故（视频含搜索"gitleaks 扫描 .env"的呼声）

### 2. 调研证据（2026年8月实搜）
- 全球检索 "API key manager local encrypted LLM"：**无独立个人级产品**；2026-04 出现 aivo CLI 把"多 provider Key 管理"作为附属功能，侧面证明需求刚出现
- 1Password 过重（$36/年、非 CLI 友好）、Infisical 28.6K★ 但面向团队/DevOps
- 弱密码检测（#57）已是 RedTail 密码软件的标配，此处不做
- 中文检索零命中——完全没有同类

### 3. 用户画像
- 独立开发者、AI 工具用户（最多的人手 5-15 个 key）、自托管玩家
- 想"一个命令就把 key 注入当前 shell/项目"的人

### 4. 产品形态（MVP）
本地 CLI（Python）：
```
kv add deepseek sk-xxx        # 加密入库
kv get deepseek               # 解密输出（可 --env 打印 export 行）
kv list                       # 只显示名称不显示值
kv use deepseek               # 生成临时 .env（不可见屏幕）或导出到 shell 环境
kv rotate-github              # 生成 GitHub Token 轮换指引（实时探测 API）
kv doctor                     # 扫本地 .env 是否存明文
```
数据：主密钥来自用户密码（scrypt KDF）+ 每个条目 AES-GCM 加密落盘 SQLite。1-口令进、1 口令出，无人能明文读文件。

### 5. 核心杀手锏（差异化）
1. **专为 API Key 设计**：模板（OpenAI/GitHub/AWS… 预置字段）、到期提醒、轮换提示
2. **与工作流集成**：`kv use` 一次性注入 `.env` 或 shell export，用完即撤
3. **泄露扫描提示**：`kv audit` 扫描工作目录中的 `.env`/硬编码提醒
4. 零依赖安全模型：主密钥永不落盘，只在内存

### 6. 边界与免责（低责任）
- 只管理开发者密钥，不管理银行卡/身份证等真实凭证（不做那种背包分类）
- 加密安全性基于本机主密钥；**主密钥丢失 = 数据不可恢复**（README 明确警示）
- 不做云同步/不传数据（除非用户显式配置自选密件区）

## 二、技术选型

| 项 | 选择 | 理由 |
|---|---|---|
| 语言 | Python 3.9+ / 或 Rust 可选 | Python 最快出 MVP |
| 加密 | `cryptography`（AES-GCM）+ `scrypt` KDF | 标准、纯 Python |
| 存储 | SQLite（表 keys: name, provider, ciphertext, created_at, expires_at） | 零配置 |
| CLI | `argparse`（click 可选） | 无大依赖 |

## 三、开发规范

见 `开发规范.md`。核心硬性：
1. **主密钥不落盘**：只封在 `os` 内存/环境变量（临时），解锁时键入
2. 加密随机盐 + nonce，每条独立；防重放
3. `kv list` 的**值得警告**：列表页永不明文打印任何值
4. 审计功能扫描 `.env` 时仅提示「找到疑似明文密钥文件」，不显示内容状态量
5. `--yes` 参数防误删；`kv delete` 必须确认

## 四、开发规划（见 开发规划.md）

Phase 1 加密核心 → Phase 2 CLI 完备 → Phase 3 audit/轮换 → Phase 4 发布。

## 五、判断标准
- 新增/查询/解密性能 <1s；文件主密钥加密后不可 grep 明文
- 断网单机可用；audit 能在 10 秒内扫 100 个 .env
- 可导出 shell 注入；支持多 provider 模板
```

### 附录 2：开发规范.md

```markdown
# 开发规范 — 个人 API 密钥保险箱

## 1. 总体约束
- **语言**：Python 3.10+；核心模块 `vault.py` ≤ 500 行
- **依赖**：`cryptography`、`sqlite3`（标准库）、`argparse`、`rich`（可选）
- **结构**：
  - `vault.py` — 加解密核心（scrypt+AES-GCM）
  - `store.py` — SQLite 持久化
  - `cli.py` — 全部子命令
  - `audit.py` — .env 明文扫描
- **安全红线**：
  - 主密钥**永不写盘**；只在解锁后的进程内存
  - 密文库用 0600 权限创建
  - 明文值只在**显式请求**（`get`/`use`）时 stdout 输出，`list` 永不含值

## 2. 加密模型
- 初始化：`kv init` → 用户输入主密码 → `scrypt(N=2^15, r=8, p=1)` 派生 32B key + 随机 salt 存入库头
- 每条 entry：`AESGCM(key, nonce=os.urandom(12))` → `ciphertext` 且独立 nonce；`associated_data = name+provider` 防篡改
- 存储：SQLite 表：
```
secrets(id, name TEXT UNIQUE, provider TEXT, ciphertext BLOB, nonce BLOB, created_at, expires_at, notes)
```
- 解锁：`kv unlock --passphrase <env:KV_PASS>`；进程退出锁自动失效

## 3. CLI 接口（硬性）
```
kv init                    # 创建 vault，要求主密码 ≥8 位（不强制复杂但提示）
kv add <name> --provider deepseek [--expires 2026-12-01]  # stdin 或 --v 取密钥
kv get <name>              # 解密，打印...仅显示后 4 位（--full 才全显）
kv list                    # 名称/供应商/过期日期（永不有值）
kv use <name>              # 输出 APP_KEY=xxx 导出行（供 eval / 注入 .env 文件）
kv edit <name>
kv delete <name>           # 需二次确认
kv audit --dir .           # 扫描 .env/配置明文密钥
kv rotate --provider github # 打印该供应商轮换 URL（模板化）
kv export-backup <file>    # 导出加密备份
```
- `--yes` 只能用于已确认安全的 `delete`/`rotate`；禁止无条件批量删

## 4. `use` 注入设计（安全）
- 默认打印 `export DEEPSEEK_API_KEY=sk-xxxx` 行（`--shell bash/zsh/fish` 自适应）
- 支持 `--dotenv out.env`：写入 0600 权限的临时 .env，用完删；提示不再明文留
- **禁止** `use` 直接 print 值到被 shell history 捕获的场景（记录到 README）

## 5. audit 扫描
- 递归扫 `.env*`、`*.ini`、`*.toml` 的 `KEY=value`，探测常见 key 名（OPENAI/DEEPSEEK/GITHUB_TOKEN…）
- 命中 → 只显示文件名+key 名+一段明文（或 `:REDACTED`），绝不打印密钥值
- `--fix-0600` 可选修复权限
- `--no-write` 默认只报告

## 6. 容错与安全
- 主密码错误：提示"无法解锁"（不泄露 salt 存在性差异）
- 写入先写到 `DEFAULT CACHE` 再原子 rename（防半写）
- 异常时密文绝无明文备份

## 7. 测试
- `tests/test_vault.py`：init/unlock/list/get/delete/audit；**密文找不到明文 key**、nonce 独立性、篡改检测
- `tests/test_audit.py`：制造带 .env 的样例目录，命中报告正确、不打印值
- `tests/test_cli.py`：参数/输出格式/二次确认
- `pytest` 全绿（`-v`）

## 8. 交付物
- CLI 可运行 + README（安装、使用、安全模型、免责：主密码丢失不可恢复）
- `pyproject.toml` 安装为 `kv`
- 示例演示截图
```

### 附录 3：开发规划.md

```markdown
# 开发规划 — 开发密钥保险箱 (KeyVault)

> 周期：2-3 天。优先保"加密正确性"而非功能多；可先用测试驱动。

## Phase 1（半天）：加密核心（TDD）
- [ ] `vault.py`：scrypt 派生 + AES-GCM 加密/解密（独立 key/独立 nonce）
- [ ] `store.py`：SQLite 表 + init/unlock（0600 权限）
- [ ] `tests/test_vault.py` 绿：加解密往返、篡改报错、nonce 独立
- 验证：`kv init` 创建文件，`kv add/get` 往返还原且密文内搜不到明文

## Phase 2（半天）：CLI 骨架
- [ ] 全部子命令（init/unlock/add/get/list/use/edit/delete/export-backup）
- [ ] `use/env` 注入两模式（shell 行 / .env 文件）
- [ ] `list` 永不显示值；`delete` 二次确认
- [ ] `tests/test_cli.py` 绿
- 验证：`kv use deepseek` 输出 export 行；audit 目录无明文残留

## Phase 3（半天~1 天）：audit + rotate + 体验
- [ ] `audit.py` 明文扫描（报告不外泄值）
- [ ] `--expires` 过期提醒（到期 get/list 时显示 IM过期标识）
- [ ] `rotate` 输出轮换指引 URL
- [ ] 个性化：`kv shell-init` 生成 shell alias（kvg = kv get --env）
- 验证：造 2 个含明文 key 的示例 .env，audit 正确命中并只输出文件名

## Phase 4（半天）：发布
- [ ] README：安全模型（scrypt+AES-GCM 怎么工作）、主密码丢失=数据不可恢复警示、示例全流程
- [ ] `pyproject.toml`、`pytest` 全绿
- [ ] GitHub 仓库 + 录屏演示

## 验收清单
- [ ] 密文文件 grep 不到任何明文字符
- [ ] 主密码错→失败提示；`list` 无可读值
- [ ] 备份导出→还原验证成功（关键！）
- [ ] audit 输出不含密钥值

## 风险与对策
| 风险 | 对策 |
|---|---|
| 主密码丢失 | 界面醒目警示 + 导出加密备份指引（同上再强调） |
| shell 历史泄露 | use 默认不打印；建议环境变量或 .env 注入 |
| 加密库版本不兼容 | requirements 锁定版本 + 兼容 Python 3.9-3.13 |
```

### 附录 4：架构设计.md

```markdown
# 架构设计 — API 密钥保险箱（KeyVault）

> 目标：加密核心与存储/CLI 彻底分离；密码学模块无副作用；审计模块只读。

## 1. 总体分层

```
┌─────────────────────────────────────────────┐
│              cli.py (子命令路由)            │
├───────────┬─────────────┬──────────────────┤
│ vault.py  │  store.py   │   audit.py       │
│ 加密核心   │ SQLite/IO   │ 明文扫描(只读)   │
├───────────┴─────────────┴──────────────────┤
│           config.py（路径/权限/加载）        │
└─────────────────────────────────────────────┘
```

依赖方向：`cli → vault/store/audit`；`vault → (cryptography)`；`store` 不 import `audit`。
**禁止出口**：`vault` 不打印明文；`audit` 不写任何文件。

## 2. 接口契约

### 2.1 `vault.py`（密码学核心，纯函数）
```python
SCRYPT_N = 2 ** 15; SCRYPT_R = 8; SCRYPT_P = 1
KEY_LEN = 32; NONCE_LEN = 12
TAG_BYTES = 16

@dataclass
class VaultHeader:
    version: int = 1
    salt: bytes               # 16B
    kdf: str = "scrypt"

@dataclass
class EncryptedEntry:
    name: str                 # 明文：仅名称（非密文）
    provider: str
    ciphertext: bytes
    nonce: bytes              # 独立 nonce/条目
    created_at: str
    expires_at: str | None

# ---- 纯函数（无 IO）----
def derive_key(password: str, salt: bytes) -> bytes
    # scrypt(password, salt, N,R,P) -> 32B；耗时 ~0.3s（可接受）
def encrypt_entry(key: bytes, name: str, provider: str,
                  value: str, expires: str | None) -> Encrypted
def decrypt_entry(key: bytes, entry: Encrypted) -> str
    # 失败抛 IntegrityError（nonce/tag 校验）
def payload_from(cipher: bytes, nonce: bytes, aad: bytes) -> ...
```

### 2.2 `store.py`（持久化，格式：`secrets.db` 单文件 + 可导备份）
```python
class VaultRepo:
    def __init__(self, path: str)
    def exists(self) -> bool
    def init(self, header: VaultHeader) -> None      # 建库, chmod 0600
    def insert(self, entry: Encrypted)
    def get(self, name: str) -> Encrypted | None
    def list_all(self) -> list[Encrypted]
    def delete(self, name: str) -> bool
    def update(self, name: str, entry: Encrypted) -> None
    def export(self, out_path: str) -> None          # 全库序列化（含 header）
    def import_from(self, in_path: str) -> None      # 原子替换
```
- **原子性**：写前写临时文件 `secrets.db.tmp` → `os.replace`；异常中途删除 tmp

### 2.3 `audit.py`（只读扫描）
```python
KEY_NAME_PATTERN = re.compile(r"(OPENAI|DEEPSEEK|GITHUB|AWS|AZURE|GOOGLE|API)_?KEY|TOKEN|SECRET")
@dataclass
class Finding:
    file: str
    key_name: str
    redacted: bool          # 默认 True 代替值

def scan_dir(root: str, *, max_files: int = 10_000,
             ignore: tuple[str, ...] = (".git","node_modules")) -> list[Finding]
    # 只报 .env*/.ini/.toml/config 后缀；值区打码 `REDACTED（前4位…后2位）`
def scan_envline(line: str) -> Finding | None   # 纯函数，单行判定
```
`--fix-0600`（可选开关由 cli 控制，audit 本身从不写）。

### 2.4 CLI 子命令契约
```
kv init                     # 交互密码≥8 → create header
kv add <name> [--provider p] [--expires YYYY-MM-DD] [--stdin]
                            # 无 --stdin 则提示从 stdin 读值（防 shell 历史）
kv get <name> [--full]      # --full 全显; 否则打印 "sk-…****h4" 摘要
kv list                     # name | provider | expires (无值)
kv use <name> [--shell bash|zsh|fish] [--dotenv out.env]
kv edit <name>              # 交互改值/到期日
kv delete <name>            # 需"yes"
kv audit [--dir .] [--json]
kv rotate --provider github # 打印官方 rotate URL 指南
kv export-backup <path> / kv import-backup <path>
kv unlock  # 密码经 stdin/env KV_PASS；成功后进程内存持有 key，退出即弃
```
`--yes` 仅允许 `delete`/`rotate`；禁止批量无条件删除。

## 5. 数据流（一次 add）
```
cli init/unlock → vault.derive_key(密码+盐) → 内存 key
add → vault.encrypt_entry → store.insert(原子 write)
get → store.list → vault.decrypt(event) → 摘要/全显
audit → scan 目录（只读）→ 打码报告 → 结束（不触碰 vault）
```

## 6. 并行开发切分
- **AI-A**：`vault.py`（密码学）→ 率先冻结（全项目依赖它）
- **AI-B**：`store.py` 原子 IO + 测试（不依赖 vault 类型桩定义即可并行？→ 用 Encrypted 类型占位）
- **AI-C**：`audit.py` 只读扫描 + 测试
- **AI-D**：`cli.py` 组装 + `config.py`
装配：A→(B,C) 并行→D。

## 7. 接口核对清单
- [ ] `derive_key` 同密码+盐幂等；different盐→不同 key（测试）
- [ ] 篡改 ciphertext/tag → `decrypt` 抛 ValueError
- [ ] `audit` 输出与文件内容 0 写
- [ ] export/import 往返成功且校验 header
- [ ] list 输出永远无明文（扫描测试打 value 断言 None）

## 8. 安全底线（架构级，不可绕过）
- key 仅存进程内存；`os.urandom` 生成 salt/nonce
- 任何写操作前 perms 校验；`.db` 权限 0600
- 不提供"显示所有明文"的批量功能（防误操作/泄露）
```
